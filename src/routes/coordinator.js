export const goToSignup = (history) => {
    history.push('/signup')
}

export const goToLogin = (history) => {
    history.push('/login')
}

export const goToIndex = (history) => {
    history.push('/')
}